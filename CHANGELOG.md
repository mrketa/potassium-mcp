# Changelog

## Unreleased
- Fixed Windows PowerShell 5 child processes inheriting incompatible PowerShell 7 module paths. Broker process inspection and installer ACL preservation now rebuild the child module path while retaining explicit operation variables. A real CLI regression preserves private bytes and ACLs under an incompatible inherited Security module. The bounded OS process-inspection allowance is now five seconds for observed cold CIM startup; RPC, proxy-handshake and lifecycle deadlines are unchanged, with no retries or cached ownership.
- Pinned repository SDK selection to .NET 8.0.424 and corrected hosted qualification fixtures for canonical temporary paths, cross-drive relative identities, actual entrypoint links and bounded native-observer cleanup.
- Updated locked production transitives `fast-uri` 3.1.5→3.1.7, `hono` 4.13.2→4.13.7, and `qs` 6.15.3→6.16.0 to address reported security advisories within existing dependency ranges; direct dependencies and the package version are unchanged.
- Fixed proxy startup socket-error races across WebSocket upgrade, authentication phases and the forwarding handoff. Continuous startup ownership preserves the original error, closed sockets reject promptly, and authentication waits for successful send completion before consuming MCP input. Invalid proof/protocol frames remain terminal; no retry, replay or timeout extension was added. Real same-read wire faults now reject instead of crashing the proxy. This source fix does not retroactively identify the earlier unrecorded Node24 incident or update archived package/installer bytes.
- Retired the optional MCP usage skill and consolidated maintained guidance into the existing docs: [Agent quick start](docs/AGENT-INSTALL.md#agent-quick-start) links the canonical [Agent workflow](docs/API.md#agent-workflow); tool schemas and structured errors remain authoritative. No default skill installation or skill-evaluation prerequisite for Stable remains. A future thin linked router is conditional on demonstrated need. The earlier skill addition/evaluation entries below remain historical; exact former skill/reference/case inputs are archived in [SKILL-SNAPSHOT.json](release-out/first-stable-source/SKILL-SNAPSHOT.json), with [SKILL-EVALUATION.json](release-out/first-stable-source/SKILL-EVALUATION.json) unchanged and not relabeled as Docs-vs-Skill proof. This documentation/source-inventory change does not requalify native, Node, package or installer behavior.
- Fixed compact input schemas rejected by the real mounted host as unresolved `#d0` references. Generated input references now use local JSON Pointers, preserving declared scopes, runtime constraints and existing discovery limits. The formerly rejected result-pointer call and recording archive/save-receipt readers passed through the actual host after a controlled broker-only restart.
- Qualified one real lifecycle-5 idle recording:571frames/1713samples from three character parts over29.986seconds, a request-timed marker, natural stop, complete archive retrieval and durable save retry. Root position stayed unchanged; head/torso animation was observed; Health100. Recorder resources, references and native snapshots returned to zero. This is selected idle-observation evidence, not a parkour-run or safety guarantee. Node22/24 each842tests/839passed/3skips; no package/installer build or publication.
- Added source-side continuous map recording (`mapRecording1`, bootstrap `lifecycle-5`) with first-real-sample readiness, an owned sampler independent of polling, native monotonic frame/marker clocks, bounded retention, explicit misses/partial reasons and lifecycle cleanup. Markers timestamp MCP receipt, not physical input. This entry does not claim new native acceptance, bootstrap deployment or a promised user run.
- Added terminal-only once-only recording import into immutable schema3 maps, full offline frame/event archives and durable acceptance receipts published under the same map index boundary. Exact accepted retries remain offline across native expiry/restart and parent release; a released saved map returns its original identity rather than being recreated. Existing schema1/2 bytes and floor/ceiling, activation and unknown-time semantics remain.
- Added exact section-compatible offline map selectors, query-bound pagination and explicit compact track summaries; continuous motion models use labeled bounded projections while retaining full raw evidence. Ordinary graph links group only exact-equivalent semantics and preserve every discrete timing window; rendering reports represented and omitted paths/opportunities. No final performance or evaluation improvement is claimed by these source changes alone.
- Split public map APIs without aliases: `potassium_map_navigation` retains links/routes, while `potassium_map_motion` owns tracks/hazards and summaries. `potassium_map_recording` retains lifecycle/summary polling; `potassium_map_recording_read` owns live frame/event polling and offline archive reads. Strict contracts and existing discovery/result limits remain; final SDK size qualification is recorded separately.
- Added an optional manually installed Potassium MCP usage skill for scoped offline-first evidence, typed discovery, bounded recording/results and permission/error handling. It does not overwrite host assets, auto-register MCP, grant permissions, enable unsafe execution or certify host integration.
- Required explicit `--npm-artifact` and `--output` for version qualification, reusing the shared adjacent receipt/tarball/checksum verifier and recording the selected identity. Documented first-Stable scope and separated historical npm/EXE/ZIP evidence from current source work. No new package/installer build, reinstall, version bump, signing or publication is authorized by this change.
- Tightened recording model/persistence boundaries: preflight the combined128-track quota without archive overwrite; order same-generation continuous windows by actual native sample time, keep mixed-clock ordering uncertain, and forbid target-lost/failed extrapolation while retaining valid raw evidence. Same-object recapture preserves historical snapshot provenance. Exact same-process imports coalesce within eight in-flight imports/configuration; owner acceptance survives joined cancellation, with cross-process BUSY and durable retry remaining explicit.
- Qualified the integrated source on Node22 and Node24 with841 tests each:838 passed, zero failures and three platform skips. The modeled bootstrap audit passed134/134, including the100-cycle lifecycle scenario. These are source/model results only, not new native continuous-recording acceptance, package/installer qualification, deployment, performance/evaluation improvement or publication.
- Recorded source-only end-to-end evidence for601native-modeled frames/1803samples across30seconds, immutable archive equality through two independent SDK readers, and offline accepted retries. The smoke explicitly models documented HttpService empty-table JSON behavior; it does not qualify a live engine.
- Measured exact graph grouping on preserved Galaxy inputs:1002→110 and1157→128 stored links, with every discrete opportunity and all parts/surfaces/tracks/hazards retained; link JSON is roughly82% smaller. Existing map bytes are unchanged and the final Windows map image was visually checked. No further cross-revision cache was added without proven benefit.
- Measured offline selected reads on the same saved map: both reported pads11→1read and59839→2587DTO bytes; five track summaries5→1read and30378→3092bytes. Selected rows/summary facts match the full reads, with raw samples still accessible; these are service DTO measurements, not network/token estimates.
- Evaluated the optional skill in90 inert decisions. A small recording-archive routing benefit remains after rubric caveats, but unnecessary refusals were zero in both arms, so no refusal-reduction claim is made. Source/model/evaluation evidence is in `release-out/first-stable-source/`; current package/installer/live-recorder gates remain open.
- Added signed floor/ceiling support, mirrored clearance/ballistics, and explicit user-reported mode-switch mechanics in immutable schema2 map revisions. Legacy map bytes remain readable/releasable. Switches are candidate transitions with unknown/null timing, never invented gravity laws or zero-time teleports; ordinary routes cannot ignore reported activation regions. Offline mechanics, typed16KiB discovery, large-coordinate overlap precision and prioritized mode labels were verified.
- Qualified the mode extension with Node22/24 full suites each786tests/783passed/3platform skips, followed by final activation-guard navigation/service verification57/57 on both. The saved Galaxy2 Stage1 map now represents10floor/10ceiling faces and two reports; the installed candidate itinerary changes floor→ceiling→floor with unknown total timing. No new user run, bootstrap replacement, package/installer build or publication.
- Added offline-first parkour reconstruction with full3D stand-surface candidates, walk/jump/passive-drop/ride/wait links, sparse chunks, evidence-qualified hazards and immutable shared revisions. Separate management/geometry/navigation tools retain strict typing within existing16KiB discovery frames. The new lifecycle-4/gameContext2 bootstrap adds bounded identity-owned motion sampling and downward probes; it does not automate movement or invoke remotes. Original captured geometry remains readable and releasable using its original verified bytes.
- Qualified the reconstruction source with Node22/24 suites each750 tests/747passed/3platform skips,122 modeled bootstrap cases, and a real producer-to-store-to-two-SDK-reader Windows map-rendering smoke. Final drawing labels received separate20/20 verification on both Node versions. Existing real saved game data also reconstructs offline; actual lifecycle-4 engine observation/probe acceptance remains pending a normal Attach, not a separate testworld.
- Added config-owned shared game context with static Workspace geometry, PlayerGui information, ReplicatedStorage remote identities, bounded Windows window-only screenshots and rendered top-down maps. Contexts persist across agents/session/Roblox closure, with bounded ownership-safe storage, explicit image retrieval and release; snapshots do not invoke remotes or move the camera/player.
- Reduced tool overhead with summary/section capability views, explicit structured-results presentation, auto typed result-pointer defaults, retained inventory queries and exhausted-traversal short-circuiting. Connection diagnostics now separate authenticated disconnects from handshake failures. Strict shared schemas remain valid within existing discovery/transport bounds; legacy text paging is explicit.
- Fixed orphan native WebSocket retries after peer closure: the bootstrap now explicitly retires the old transport before its Lua reconnect loop opens a replacement. An owned native-loopback probe observed29 physical connections from one unretired Lua socket versus two, including one racing reconnect, with explicit retirement. The old source fails the new lifecycle regression; corrected bootstrap89/89 passes. Build marker is `lifecycle-2`; full process restart and active-path acceptance remain required for already-affected sessions.
- Fixed detached broker shutdown silently accepting a failed final status publication. A bounded final-state recovery and per-attempt ownership checks now precede the stopped receipt; persistent denial fails shutdown and observed replacement generations are preserved. Three regressions fail the original source and pass the fix; real Windows sharing-lock probes reproduce five native EPERMs before both the old stale-state outcome and corrected final-state publication.
- Removed the separate Studio/server testworld workflow at the user's request, including its server/client scripts, standalone runner, dependent tests, release inventory entries, and active documentation/planning prerequisites. Production MCP tools and ordinary regression coverage remain unchanged; archived measurements are not promoted into new verification claims.
- Added shared-budget multi-pointer result retrieval with direct small values, bounded session usage metadata, temporal action observation, focused diagnostic views, and typed single remote jobs. Queued cancellation prevents dispatch; cancellation after dispatch preserves eventual native returns/errors.
- Added supplied-source offline Luau indexing with a real native Tree-sitter parser, bounded validated tree adaptation, lexical callsite/dependency/origin evidence, scoped retention and separate source-root controls. Source is parsed, never executed; richer module execution/scenario tooling remains separate.
- Isolated the parser in a standard no-capability Windows AppContainer with atomic Job process/memory/CPU/lifetime bounds. A GUI-independent native worker avoids extra runtime dependencies. Job-only child confinement replaces an incompatible redundant Windows child-policy attribute; actual OS denials, resource limits and recovery were qualified without modifying existing UI ACLs.
- Removed advisory tool-description/error prose while preserving technical codes, uncertainty, permissions and truthful annotations. On the same fixture, three detail calls/3,206 bytes became one call/2,384 bytes; description text shrank from6,321 to4,832 bytes. The full typed catalog grew from51/81,026 bytes to56/88,676; these are measured JSON bytes, not universal token savings.
- Qualified the historical expansion with559 passing Node tests and three platform skips,70 Lune cases,28 Setup tests,22 tooling tests, real npm/npx lifecycle and final-package SDK parsing on Node22/24. These are artifact-bound historical results, not automatic qualification of later source changes or unexercised server behavior. No version bump or publication follows.
- Added remote inventory v2 with compact grouped summaries, projected rows, generation-bound snapshots, replayable cursors, explicit partial coverage, bounded diffs and release. Selected instance references use the existing registry without rescanning cached pages.
- Added explicitly started, bounded outbound remote-namecall metadata capture with type-shape aggregation, replayable polling, deadlines and ownership-aware cleanup. No argument values, return values, guessed remote calls or automatic replay. Native helper mechanics were checked only with local functions/disposable userdata; actual game interception and in-place closure-hook interference remain unqualified.
- Capped complete tool results at 8 KiB or the stricter configured limit, with original-schema validation, scoped expiring result pages, UTF-8-safe offsets, optional JSON-pointer selection and originating-permission rechecks. Trace summaries omit rows by default; normal small-result shapes and accepted execution identities remain intact.
- Added explicit compatible-client lazy tool discovery, typed catalog activation and standard tools/list pagination under the unchanged MCP frame cap. Ordinary clients retain the full catalog; the pinned SDK requires explicit page handling and caches only its latest page.
- Verified 476 Node tests with two platform skips, 55 isolated Lune cases, 28 C# tests, nine packaging tests and real npm/npx/three-transport lifecycle. On the same 100-row fixture, first summary response fell from 81,952 to 752 bytes; lazy catalog is 5,047 bytes while the full catalog grew from 57,956 to 81,026 bytes. These are measured JSON bytes, not universal token or full-fetch savings. Current local artifacts remain unsigned/unpublished.
- Enabled full read/admin/execute access and the global Luau execution gate by default for genuinely fresh Windows installations, without a consent/permission prompt; removed the Advanced folders section and placed workspace selection inline. Repository security documentation explains arbitrary execution and trusted-agent risks.
- Added initialization-only `--initial-full-access-host` semantics after transaction recovery, preserving existing/retained restrictions instead of overlaying grants. Missing configuration or a pending recovery journal no longer causes Windows Setup to precompute an explicit read grant over potentially restored restrictions.
- Verified the full-access build with 442 passing Node tests and two platform skips, 28 C# tests, nine packaging tests, actual fresh GUI installation/repair/connection, and installed SDK discovery of 46 tools. A real previous restricted installation upgraded/repaired through the new GUI retains its token and 36-tool nonexecution policy. No arbitrary Luau execution or publication was performed.
- Rebuilt the separate Windows Setup.exe as a harness-independent installer: no Desktop/CLI or permissions chooser, no Safeproxy, no automatic host registration. The durable self-contained launcher uses integrity-checked bundled Node/MCP and emits a token-free connection entry; repair, update, and removal preserve private state and refuse foreign ownership.
- Qualified the local Windows installer with 27 C# tests, nine packaging boundary tests, actual Windows 11 GUI lifecycle checks, cold connection checks, and stock-SDK discovery through its installed launcher. Fixed child-environment ACL identity and inherited stdio handles; existing user installation and wrapper remain unchanged. The artifact is unsigned and unpublished, with native engine acceptance still separate.
- Replaced the coupled MCP installer with public `serve`, hostless external-package `setup`, read-only `config print`, and optional host-only registration/removal. Schema-3 ownership preserves private credentials/configuration, distinct project policy IDs, explicit path precedence, rendered loopback endpoints, and effective request deadlines; dry-runs perform no mutations.
- Added bounded, validated installation journals and shared startup leases for process-crash recovery. Committed startup uncertainty retains the new credentials instead of rolling them back under a possible live broker; post-commit cleanup failures remain cleanup work. Legacy junctions and user-managed launchers require proved, deliberate migration and are never blindly overwritten.
- Hardened HTTPS DNS pinning, opened-file identity checks, artifact publication/pruning, structured redaction, and metadata-only audit replay. Independent admin/read/execute policies, typed error envelopes, and content-addressed output schemas preserve meaningful failures without unsafe replay guidance.
- Fenced broker shutdown through executor recovery, accepted responses, and actual handler/audit completion. Public transport ID normalization supports zero/empty IDs safely; mixed HTTP cancellation groups remain session-scoped and bounded, and terminal shutdown cannot be reopened by an abandoned management lease.
- Qualified the local MCP runtime on Node 22.23.2 and 24.15.0: each full suite passed 440 tests with two explicit platform skips. Real packed npm/npx, active compatible-version transitions, edited-configuration repair, and OMP-native discovery were exercised without changing the user's installed wrapper or live bootstrap; native engine acceptance and publication remain separate gates.
- Hardened MCP bootstrap socket ownership: duplicate request IDs and uncertain response sends now fail closed without competing outcomes or premature execution-lock release; teardown releases log observers immediately. Four behavior-only regressions fail against the previous deployed source and pass against the corrected isolated source.
- Added a checked-in Lune 0.10.4 bootstrap runner with real JSON codecs, visible engine limitations, queue/benchmark modes and explicit toolchain failures. The 38-case core and combined 100-cycle lifecycle pass with 1,024 references, 16 watches, 8 active jobs, and zero remaining fixture connections/timers; no new live bootstrap deployment or engine-fidelity claim is implied.
- Fixed false-stale broker identity for installation junctions using native canonical paths and exact managed process arguments. Lifecycle operations recheck ownership before signaling and accept verified proxy-started replacement brokers without treating a competing startup as failure.
- Added bounded mixed MCP batch reads with per-target/facet errors, ordered partial results, dynamic value limits, and hard response/serialization/work budgets; retained the existing property-only batch API.
- Added opt-in stable instance references to discovery, inspection, and mixed batch summaries. References work through existing Roblox target-path consumers, survive rename/reparent/detach/reconnect, and fail explicitly after destruction/release/generation replacement. The 1,024-entry registry includes tombstones, supports bounded explicit release, and preflights capacity/result size before transactional allocation.
- Added persistent MCP watches with replayable cursor pages, explicit overflow counts, count/byte-bounded buffers, idle expiry, and listener cleanup on stop, destruction, and bootstrap teardown.
- Added async job metadata listing and cooperative cancellation with distinct cancelled results, queued-work removal, and bounded connection ownership. Running cancellation never releases the raw-execution lock before the chunk exits. A bounded lifecycle control lane remains usable behind ordinary mutation barriers and a saturated ordinary request queue; timeout recovery remains fail-closed.
- Reverted the Galaxy2 Stage10 full-speed exception and fixed 2.25-second final hold after the user reported intermittent invalidations. All Galaxy2 routes again consume the native minimum/debounce-plus-margin budget, with the final hop waiting its nominal distance/scaled-speed duration. Successful short trial batches were insufficient evidence of reliable farming.
- Restored native-timing regressions fail the optimized runtime and pass the corrected version; the existing runtime smoke and all 110 audit cases pass. Corrected source is synchronized and loaded in the stopped client, preserving preferences. No failing live route was rerun and no new long-run reliability claim is made.
- Optimized Galaxy2 Stage10 stop timing: 300-stud/s travel, nominal 0.2-second SAS sampling, and a fixed 2.25-second SAS10 hold before the single SAS11 hop replace the previous 30-second route stretch. Lower Galaxy2 targets, Stage1, and World3 retain their timing behavior.
- Three immediate live cycles with the accepted timing paid 11,800 Wins in 20.246/20.321/20.140 seconds per cycle, with no warning, Health100, and retained balance after six seconds. Shorter 0.1- and 1-second holds failed their third repeat and were rejected. Updated before/after regressions, the runtime smoke, and all 110 audit cases pass. Local runtime updated; no public-loader publication.
- Reverted the Galaxy2 Auto Wins underpass route after the user reported no Wins. Restored the previous SAS route, Stage8 wall-opening path, and original timing calculation; the Stage10 final hold/TP remains unchanged.
- Added Galaxy 2 World 1 support to the existing Keyboard Escape entry, with native World4 configuration, actual stage indices, streaming-aware SAS routes, reversible hazard removal, and gravity-compatible Godmode. The original World3 profile and all existing controls remain.
- Galaxy2 World1 Max Stage10 now preserves the original timing budget while holding at SAS10 and teleporting once to SAS11; earlier legs, lower targets, SAS11 dwell, and final plate contact remain tweens. World3 retains its existing checkpoint strategy through the same entry and menu. Stop, death, respawn, anchoring, strategy changes, and Destroy invalidate a pending hop and restore original collision state. All six isolated suites pass, including 110 runtime audit cases and the focused hold/cancellation regressions. Local payload updated; public loader unchanged.
- Final-hop live verification after rejoin and user-confirmed client marker: one regular shared-entry Stage10 run executed exactly one SAS10-to-SAS11 TP and earned 2,000 Wins (33,582 to 35,582), confirmed by native ShowWin feedback and real-window captures. Health remained 100 with no warning during the run; Auto Wins and Godmode were restored off. A prior connection's run had no payout, so repeatability is not claimed.
- Published the current Keyboard Escape payload to `mrketa/celestial-keyboard-escape`: main `bf1bcae6a8d513d9039fa1f19a3bb143650fd2ba`, immutable payload `62003665b4178611469960c26f00eae1ad88f389`. Public loader SHA-256 pins now include nine event choices, grouped Orbs, Chocolate/Lightning, Stage1 cooldown handling and optimized Max Stage timing. Anonymous main-loader and all five payload downloads match the release, compile and pass headless loader staging; corrupt input is rejected before writes. Roblox disconnected before publication, so published-loader app startup was not re-exercised; the identical feature payload previously passed six suites and live menu/route checks.
- Reduced Keyboard Escape Admin Events from 20 choices to nine. Removed Cheese Battle, Milk Battle, Last Summer Rings, August 15 Rings, Team Battle, and Admin Abuse Treadmill plus dead collector state/handlers. Consolidated all seven existing orb families into Orbs; WC Soccer retains the former finale's football-only behavior. Obsolete labels reject atomically; Stage1 and Max Stage behavior are unchanged.
- Event cutover verification: six isolated suites pass; actual checklist selection and thirteen obsolete-label rejections verified in the loaded client, with a direct Roblox-window screenshot. Original Chocolate Hunt selection, window preferences, and disabled automation flags restored. Native version73 update investigation recovered Allan repair/orb mechanics, LuckyMinute XP rolls and OGParty visuals, with explicit schedule/server-state limitations. Captures and evidence retained locally; no speculative new automation or public-loader publication.
- Optimized every Max Stage route (3–14) using native target-minus-previous minimum times, confirmed checkpoint settlement, and a max(50ms, RTT/2) validation buffer. Highest-stage selection, exact multiplied checkpoint debit, unresolved request ownership, and warning fail-close remain. Removed fixed 8–65s waits, the Stage-8-only timer, shared27s reward delay, extra staging Heartbeats, and the successful-loop350ms gap.
- Verification: all six isolated suites pass, and all twelve per-stage timing regressions fail the prior runtime and pass the new one. On version73, three final Stage-10 cycles took5.283–5.498s each and retained99B Wins with Health100 and no warning during six seconds after the final reward. Stage8's preliminary native-delta probe retained10B in4.375s. Stages11–14 use the same rule and have isolated timing coverage, but their rewards remain level-gated and not live-validated. Local runtime updated; public loader unchanged.
- Added `Chocolate Hunt` as the twentieth Admin Events choice. Recovered the native server's 7-stud, 0.1-second proximity collection contract and implemented nearest-pickup TP, confirmed local-user collection, late-enable visual discovery, packet/visual deduplication, session-safe cancellation, and bounded revisits without a collection remote.
- Chocolate verification: six isolated suites pass, including 28 new collection/lifecycle scenarios; the pre-change collector fails the extended suite. An isolated replay of the captured native server runtime produced five proximity-confirmed pickups with zero outbound collection requests. The real menu option is visible and selected, and the loaded automation waits without movement while the event is inactive. Real live-server Chocolate collection remains unverified; the public loader was not updated.
- Stage 1 Auto Wins now waits out the native cooldown before its single TP, with a minimum 10ms buffer increased to half the measured RTT. Positive Wins observations anchor subsequent deadlines; late acknowledgments, yielded reads, ping changes, Off/On, and strategy replacement cannot reuse a stale ready attempt. Max Stage timing is unchanged.
- Stage-1 verification: all six isolated suites and 45 runtime audit scenarios pass; all 15 focused timing scenarios fail the preserved old runtime and pass the new one. Four live cycles gained 2.4B Wins, with subsequent TPs 3.01155–3.01178 seconds after the preceding confirmation. Reward feedback arrived 76–276ms after TP; exact 10ms server validation is not promised. Local runtime reloaded with preferences preserved and Auto Wins restored off; public loader unchanged.
- Completed the Keyboard Escape wiring audit across all 20 controls and 19 event choices. Fixed delayed menu ON dispatch after OFF/close, Glove-method preflight, single-strategy controller enforcement, Stage-1 false reward confirmation, unaffordable checkpoint submission, and stale workers resuming after dependency loads.
- Corrected Equip Best to use owned-inventory catalogs, item purchases to honor per-player remaining stock, and Gold/Secret key telemetry to require explicit collection rather than disappearance. Fixed native event target scopes, TeamBattleSync isolation, standalone state binding, and treadmill/movement confirmation semantics.
- Verification: six isolated suites pass, including all 32 runtime audit scenarios; 13 runtime regression cases and the updated menu/collector suites fail on preserved pre-audit code and pass after repair. All 11 staged files match executor copies, and the loaded app exposes all 20 handles. Purchases/rebirths were not triggered for testing; inactive event payouts remain unverified and Team Battle remains observational. Public-loader publication is still pending.
- Fixed Auto Wins for current event restrictions and multiplier-scaled checkpoint prices. It waits while `GameplayRestrictions.StageTeleportLocked` is true, rechecks after streaming, and validates the exact native `floor(CHECKPOINTS[index].WinPrice * BonusManager:GetWinsMultiplier(player))` debit. Event-lock and 3× pricing regressions fail on the previous runtime and pass after repair; a live Stage-8 run gained 99 billion net Wins before being stopped.
- Added `Lightning Keycaps` to Auto Admin Events. The collector passively observes native LightingEvent strike/zone/claim packets, discovers already-visible electrified keys, holds physical contact for normal pickups, prioritizes feasible super-strike warnings, and counts only matching authoritative claims. Session changes, deselection, Stop/Destroy, expiry, and repeated waves are covered by the passing complete collector smoke. The local deployed collector is enabled with only Lightning Keycaps selected; live XP collection remains unverified because the event had ended. These local changes are not yet in the pinned public GitHub loader.
- Published current Keyboard Escape to `mrketa/celestial-keyboard-escape/main` at `c076271948a396d2fac950d4cc7e6a4f71ffb1bb`, replacing all twelve obsolete `potassium-next` files without rewriting history. The public `loader.lua` stages the current entry/runtime/collectors/menu/full-UI payload from immutable commit `7ee146202ff900a59f9d97b57a9a0a1e88936235` with SHA-256 checks. Eighteen UI package checks and fifteen isolated loader checks pass; anonymous public downloads match all expected bytes/hashes. No new live gameplay was run.
- Added `potassium-ui/dist/Fluxcore-RuntimeFree.zip`: a seven-module native-only Fluxcore distribution, neutral five-tab starter, local reload-safe entry, rebuildable sources, focused checks, and colleague documentation. The full/legacy entry remains separate. The 23-file archive passes eleven extracted-package checks and rebuilds byte-identically; the repository passes eighteen UI Node checks. Post-audit native library/starter/window/race checks, actual screenshots, seven-state visual comparison, and restricted archived-loader/reload/native-Close checks pass. No game code or gameplay controller is shipped; runtime-free does not guarantee freedom from kicks, bans, or executor detection.
- Audited 123 authoritative/retained production files and repaired lifecycle, cancellation/reentrancy, rollback/publication, input/focus/order, control-selection, provider, and resource-retention defects across native/advanced UI and the game adapters. Removed obsolete badge calls from Egg/Splash and refreshed Egg entry integrity hashes against its actual local dependencies. All 209 selected Luau source, fixture, and bundle inputs compile; 75 canonical offline scenarios pass, with 73 reproducing a failure on preserved pre-fix sources. Additional provider, typed-theme, and bootstrap replays pass.
- Hardened MCP read redaction/bounds, artifact ownership/retention, transport barriers/backpressure/session cleanup, and installer lock/rollback/host-policy handling. The MCP suite passes 181 checks with one Windows file-symlink privilege skip; release/deployment tools pass nineteen checks. The preserved sources fail or time out in 45 of the 47 targeted MCP regression scenarios; the other two are a preservation check and the same privilege skip.
- Measured isolated Luau CPU improvements with unchanged canonical hashes: nested-recipe hashing 5.916→5.279 ms and shared-role skin compilation 13.929→1.732 ms. A 256-job bootstrap workload retains at most 64 queue slots instead of 256. Paired native interacting frame time remains effectively unchanged at 4.1684→4.1674 ms, approximately 240 FPS at the cap; callback-dispatch CPU time is 0.824→0.889 ms. No higher-FPS claim is made. No live gameplay, real installation, publishing, or updater/operator scripts were executed during this audit.
- Completed 225 native UI and fixture-only adapter scenario invocations. Roblox exposed a repeated-freeze error in `SlotHost:_bounds`; already-frozen immutable snapshots are now returned directly without allocation. Corrected the composition-host rollback fixture to use its public Store ID. Both checks pass on the rebuilt full bundle. Seven settled visual states match within `1e-6` numeric tolerance, with a maximum shell-transparency difference of `1.49e-8`; native Home and Options-popup screenshots confirm the rendered surface. All owned client probes were cleaned up.
- Fixed first-open clipping of the Strategies popup's second option without changing its geometry or selection. RuntimeFree dropdowns now use a native Frame and the shared descendant-opacity helpers instead of animating a CanvasGroup render target. Enter/exit timings remain unchanged; interrupted reopening, search/theme/selection refreshes, and contained-popup minimize/restore have exclusive fade ownership. Before/after native screenshots verified the renderer fix; window, native race, menu, loader, and eleven Node tests passed.
- Fixed the fourteen confirmed Workflowz findings across Keyboard Escape and RuntimeFree UI. Paid checkpoint and item-purchase ownership now survives Off and bounded worker timeouts; conflicting movement, restart, and Fuse actions reject with an actionable pending reason until settlement. All movement enables share one cancellation-safe transition guard, failed workers retire their flags/resources, and legitimate teleports update Godmode's stable pose without suppressing velocity/ragdoll recovery.
- Alien collection now approaches from spawn while retaining its request-time exclusion, holds one actual ray identity through the one-second dwell, and resets stale targets. Session totals capture the pre-enable counter and survive startup refreshes, rejected actions, and superseding sessions.
- Native tween cancellation now precedes immediate state projection. Rejected toggles stay visually off, dragging owns reveal position, repeated tab selection settles page travel, interrupted Minimize/Restore preserves exact readable endpoints, and theme changes fence old tab colors. Explicit SetSize also supersedes an in-progress Restore. Focused isolated runtime/collector/menu regressions, real TweenService race tests, unchanged original UI reproductions, and passive live five-tab/screenshot checks passed; no real purchases or farming were triggered.
- Disabled Roblox automatic localization at the Keyboard Escape ScreenGui root, keeping tabs, controls, popups, and notifications in their authored English. Removed the previous item-heading-only override.
- Fixed blank Settings content after interrupted tab switches: snapshot restoration now cancels stale descendant fade tweens and synchronously restores native opacity before recapture. Added a delayed-tween regression that fails on the old bundle and passes on the repaired one. Keyboard Escape tabs now use distinct robot, inventory-box, motion-trail, halo, and sliders icons.
- Split Keyboard Escape into Automation, Items, Trails, Aura, and Settings tabs. Buyers retain their existing selectors; each equipment category now has an independent default-off Equip Best toggle. Safety and Interface controls live in Settings.
- Added per-active-session farming totals to Keyboard Escape's expanded minimized status. Wins, rebirths, confirmed key collections, fuse completions, and glove activations use resource-specific copy and lowercase compact suffixes such as `1.5m` and `1.5b`; checkpoint-cost dips never show a negative gain. Keyboard Escape danger notifications now dismiss after five seconds.
- Removed the top-right status badge from RuntimeFree and retained-mode framework windows. Deleted `StatusBadge` options/defaults, `SetStatusBadge`, `GetStatusBadgeSnapshot`, badge progress chrome, and all starter/documentation call sites; the header now reserves space only for Search, Minimize, and Close.
- Added a default-on `Minimized Status` toggle in a new Keyboard Escape `Interface` section. Disabling it removes only the lower minimized status projection; automation state and Fluxcore minimize/restore behavior remain unchanged.
- Removed the minimized pill's top-edge glow leak by fading `GlassEdgeLight` with the expanded chrome. The supersampled bridge now renders behind both Fluxcore lobes, so its deliberate endpoint overlaps cannot paint over the status outline.
- Completed the English cutover across runtime defaults, dropdown summaries (`None`/`All`), no-results copy, tests, public references, product/design documents, and historical implementation plans.
- Eliminated floating Restore frames by replacing the bridge's independent position/size tween with endpoint coupling: its bottom shares the status tween, its top follows Fluxcore geometry, and five-pixel overlaps absorb layout-frame skew. The supersampled bridge can stretch to 26px and disappears only when the panels overlap.
- Added default-off Auto Keys with explicit Gold/Secret selection and exact attribute-gated physical collection; no `SpecialKeyEvent` request is emitted, and pose/velocity/humanoid state is restored after acknowledged contact.
- Added default-off Auto Fuse with dynamic user-selected, catalog-backed normal item groups. Live `ItemsMergerClient` decompilation proved the exact five-copy `ItemAction:FireServer("Merge", key, tier)` wire. Limited/stale/unselected groups are excluded; matching `MergeSuccess` plus exact authoritative full-inventory delta is mandatory, and pending requests are idempotent and drained before stop.
- Added dedicated default-off Auto Glove Battle. Removed the generic collector's direct `SlapBattleHit` path; the controller gates synchronized combat, equips the exact Battle Glove, alternates nearest live opponents, stages and faces within the source 20-stud/28-radius geometry, activates the Tool once, waits for deferred dispatch, and restores the previous tool and complete movement state.
- Added framework-standard `Toggle.Options`: a 34px ellipsis trigger beside the switch opens the existing singular MultiDropdown as a ScreenGui portal with a hard right-opening rule. Embedded options allocate no second row, disappear immediately from active Search, anchor four pixels to the trigger's right, track owner-window movement, accept Escape from their own focused search, and inherit lock/hide/minimize/destroy lifecycle. Keyboard Escape Auto Wins, Items, and Auto Admin Events now attach Strategies, Item Variants, and Event Kinds respectively; Auto Wins limits Strategies to exactly one concurrent selection.
- Fixed Auto Buy Items stock state: validated full snapshots replace state, stock-only patches merge changed slots, and sound-only purchase feedback can no longer erase the catalog. Reconnect requires a fresh snapshot; malformed price catalogs remain mount-safe and fail closed; selected zero-stock variants report out-of-stock; Common and other selected affordable stock are eligible. A controlled live Common purchase added one item for exactly 3,000 Wins before the buyer stopped and all selections were restored.
- Sharpened and rebalanced Fluxcore typography end to end: Builder Sans won a live four-font comparison; RuntimeFree primary labels, tabs, sections, and multi values are 14px with a 16/12px header hierarchy; explicit labels use a 10px control-root/18px section inset and a shared `Y=-1` optical baseline; single-dropdown rows use 8px padding. Unified font fallbacks, the native Frame shell, integer geometry, and generation-safe visibility transitions prevent compositing and half-pixel artifacts.
- Refined the Fluxcore procedural icon language to match a lighter tool-outline reference without copying its assets: canonical strokes are 1.5 units with a 1.75-unit emphasis ceiling and preserved fractional downsampling; Trophy, Rebirth, Godmode, and Heart gained clearer low-noise silhouettes; Cart wheels were rebalanced; Admin Events now resolves to Calendar instead of the generic fallback; and the complete 16/20/24px scale chain is regression-tested.
- Changed Keyboard Escape Auto Buy progression: Trails and Auras now purchase only the immediate next eligible upgrade and never skip an unaffordable step; Items adds an all-selected `Item Variants` MultiDropdown sourced from the seven live Wins rarities and buys only selected ordinary or resolved Mysterious stock. The live REmo unsubscribe-function lifecycle is now cleaned up correctly, and malformed price/stock payloads fail closed without leaving a dead enabled worker.
- Completed the Keyboard Escape Admin Events collector: a default-off, remote-first selector with all 18 event kinds initially selected; proven legacy endpoint families and three framework logical wrapper actions; passive Survival Chase, Team Battle, and Special Keys handling; optional spatially/cadence-gated Admin Abuse Treadmill support; exact server-time WinRing unlock windows; mutual exclusion with other movement automation; and no speculative `EggRainCollect`, `SummerCoin*`, or direct Glove Battle request paths.
- Expanded the offline procedural icon catalog for Roblox automation menus: original `rebirth`, `farm`, `godmode`, and `fuse` families plus stable aliases for general controls, target selection, auto speed/win, automation, teleport, inventory, shop, rewards, combat, auto quest, and auto hatch. Spaced, hyphenated, flattened, and underscore spellings resolve without external assets.
- Polished the canonical dense-menu hierarchy without changing the framework architecture: sentence-case 13px section headers now support optional 16px icons and inset dividers; sections use a crisper material boundary; sliders expose a right-aligned formatted value above a lower 4px track while retaining the 44px hit target; standalone buttons gain deliberate 34px secondary surfaces and opt-in `Primary` hierarchy; header Search rests at 220–240px and expands responsively without crossing the six-pixel action clearance.
- Redesigned MultiDropdown as a count-first checklist popover with complete-row geometry. Attached portals keep two pixels inside the row viewport and six pixels to the rounded edge, show up to seven full rows, never expose a partial next row, shrink on narrow right-side space instead of overlapping/flipping, and reposition without rebuilding row instances. Removed the “Clear selection”/“Selection limit reached” footer copy and detached shadow. `MaxSelections=1` uses 44px interactive replacement rows.
- Added canonical runtime-free multi-select dropdowns through `Section:MultiDropdown` and `Dropdown({ Multi = true })`, including frozen capability metadata, copy-safe ordered selection values, atomic validation, silent dynamic pruning and restore filtering, plus one contained, scroll-bounded, dismissible popup with procedural chevron/check state.
- Made `Library.bootstrap` the sole canonical entry and removed the parallel `bootstrapLite` contract. Bootstrap now constructs `RuntimeFreeWindow` directly; `Library.new` and call syntax lazy-load `AdvancedLibrary` only when explicitly requested.
- Published machine-readable canonical metadata for the runtime-free Violet Workshop standard: 600×400 shell, 43px header, 130px sidebar, 44px tabs/controls, 9px body padding, 540px compact breakpoint, 20px icons, opt-in persistence, and no world blur.
- Standardized the starter template and integration examples on passive runtime-free construction. Persistence/profile storage is absent unless the caller explicitly provides `Persistence`; the default starter uses the framework-owned Idle badge, Delete menu key, native drag, local glass, local shadow, and local glow.
- Finalized the canonical navigation language: 20px supersampled procedural icons, a layered selected-icon halo, a 10% accent-tinted active surface, exactly one direction-aware slime rail, and 1px center-weighted top/side/row separators that fade fully at both ends.
- Finalized the shell material after live bright-scene review: 12.6% local shell transparency, three unclipped shadow layers, two local accent-glow layers, native small-screen font anti-aliasing with no synthetic text stroke, and no `Lighting` mutation or `BlurEffect`.
- Finalized interaction motion: 400ms shell reveal and tab emphasis; 280/220ms shared-rail stretch/settle; 600ms toggle state with 110ms restore delay; 200ms dropdown/chevron; and 600ms toast motion. Text and icons never scale.
- Made content swaps sequential, background-native, responsive, and relative to tab order. Old descendants fade directly to the real glass background in 120ms; lower targets enter from `Y=+16`, upper targets from `Y=-16`, with 220ms movement, mirrored 3px overshoot, 140ms fade-in, and 160ms settle. Page movement and descendant fades have separate tween owners.
- Replaced the standard Delete menu hotkey with Rayfield-parity header Minimize and Close actions. Minimize maps the pinned top-centre Hide/Show morph to a `−` button and adds the requested direction-bound slime stretch: 185×70 pulled to the top bar with its bottom edge fixed, elastic settle to the 185×50 pill, full-pill restore interaction, downward `width−8`/`height+20` restore stretch with the saved top edge fixed, saved geometry restoration, shifted status badge, and no drag interception while collapsed.
- Established Fluxcore as the canonical user-facing Bootstrap identity. The 32px procedural Ion Gate remains at `X=12`; expanded and minimized shells now place `Fluxcore` at `X=54` with the adapter-provided game name directly underneath, replacing both the old game-only header and the minimized `Tap to show` hint.
- Promoted the header identity into the reusable library standard `Library.Brand = { Name, Icon }`, with frozen machine-readable defaults in `Library.Framework.Brand`, validation for malformed templates, starter-level `BrandName`/`BrandIcon` overrides, immutable resolved `UI.Brand`, and theme-safe alternate built-in icons. Fluxcore plus `iongate` remains the zero-config default.
- Expanded the Steal an Egg adapter to Runtime v5 with independently implemented Auto Steal targeting: true multi-select area/category/rarity/mutation filters; live rarity rank and KG derivation from `Data.Assets` when field records expose only `AssetCategory`/`AssetScale`; minimum- and big-egg weight filters; deterministic nearest/rarity/weight priorities; bounded 16–810 movement speed with exact WalkSpeed/root-anchor restoration, a 5–20 second per-leg timeout, a calibrated 4-stud interaction arrival radius, and origin recovery after failed carry or movement attempts; trap/treadmill safeguards; selectable return destinations; synchronous observed auto/manual sale; one Fluxcore target highlight; restored place/hatch/equip stages; opt-in local God Mode lifecycle; transient-only reconnect that refuses enforcement kicks; fixed local-launcher teleport queueing; atomically validated `fluxcore-steal-config` v1 import plus fixed-path export/save/load/reset; copy-safe status snapshots; and complete runtime-owned cleanup.
- Reworked Keyboard Escape `Max Stage` Stage 8 into the validated checkpoint-6 shortcut: the runtime guards stream/cancel before the 7-billion cost, follows checkpoint-spawn/pre-bridge/bridge-commit/staging, commits at request+5.25 seconds, and makes one 300-stud/s WinBlock39 approach. The attempted 3.5-second reduction could show an immediate Wins delta but was later invalidated by the game's obby validator, so it was removed. The corrected live cycle rewarded at 5.511 seconds for 17 billion gross and 10 billion net, retained Health 100, produced zero `CheatWarningEvent` packets across a six-second post-reward observation window, and repeated another valid cycle. `CheatWarningEvent` now fail-closes Auto Wins with a 30-second retry block.
- Standardized framework MultiDropdown motion presets: `multiDropdownEnter` is 0.20s Exponential Out and `multiDropdownExit` is 0.16s Exponential Out. Retained-mode custom Presence and reduced-motion support remain available; RuntimeFree uses the fixed defaults across its container, panel, stroke/shadow, search, and chevron stack without row transforms, while single dropdown behavior is unchanged.
- Debloated the Keyboard Escape menu by removing every read-only status, level, and max-stage row while preserving all eight interactive controls. Exhaustive live mapping confirmed Stage 1 instant teleport; Stage 2/3 via checkpoint 1; and Stages 4–14 via checkpoints 2–12 respectively. The Level-690 high-stage attempts were not mapping failures: the server's explicit unmet-level responses validate those pairings conditionally. Max Stage now follows the live World 3 gates through Stage 14 instead of ignoring them.
- Fixed a reveal/minimize tween race that could leave the restored 600×400 shell at `GroupTransparency=1` and `BackgroundTransparency=1`, producing only an empty outline. Minimize and Restore now settle the shell visibility invariant before geometry tweens can cancel an in-flight reveal and again at restore completion.
- Added generation-safe restoration for interrupted content swaps, Search-owned tab navigation, selected-tab halo transfer, status-label optical centering, local effect clipping, shared-rail direction, faded separators, supersampled icons, runtime-free bootstrap, and starter-standard regressions.

## 0.10.0-beta.1 — 2026-08-20
- Completed the MCP expansion: four concurrent reads per executor client with FIFO mutation barriers, heartbeat/reconnect, explicit multi-client routing, per-host and HTTP capability policies, stateless `/mcp`, optional bounded `/mcp/session`, artifact-backed large async results, diagnostic-only fixed-port built-in fallback, repair-preserved configuration with explicit revocations, and ownership-gated token rotation requiring reattach.
- Documented async job schemas, retention and console-capture caveats; policy enable/revoke/verify commands; separate fallback-token requirements; artifact retrieval; and before/after counter verification.
- Made async-job console capture reliable on Potassium 2.4.3 by wrapping each job chunk's `print` and `warn` environment locally, while preserving bounded argument capture, redaction, original console output, and the `LogService` fallback.
- Added an explicitly opt-in `potassium_execute_luau` trusted-admin tool, disabled by default and enabled only by `--allow-unsafe-execute` during install or repair.
- Added executor-backed asynchronous Luau jobs with immediate bounded admission, FIFO execution, reconnect-persistent status/result polling, terminal retention limits, indeterminate-submit protection, and metadata-only audit linkage.
- Added opt-in admin status, bounded durable audit history, and compare-and-swap transport recovery around trusted Luau execution without claiming forced cancellation.
- Added ownership-verified `potassium-mcp broker status|restart` lifecycle commands with live PID, version, configuration digest, readiness, and active-operation reporting.
- Added one authenticated broker plus per-host stdio proxies; scheduling now permits four concurrent reads per executor client while mutations remain FIFO barriers.
- Added explicit OMP, Codex, Claude Code, Claude Desktop, VS Code, Cursor, Gemini, and generic manual host adapters with per-host ownership-safe install, repair, doctor, and selective uninstall.
- Preserved JSONC comments, foreign JSON/TOML content, exact owned-entry checks, transactional rollback, bounded proxy startup, and token-free host configuration.
- Prepared the public npm package `@mrketa/potassium-mcp` with the `potassium-mcp` executable and npx-only install, repair, doctor, and uninstall commands.
- Removed GitHub Packages and legacy PowerShell setup/uninstall flows from public installation and release publishing.
- Converted the public package to a standalone Potassium MCP bridge for bounded local inspection.
- Removed gameplay automation and runtime deployment surfaces.
- Deployment now transactionally installs only canonical bootstrap and autoexec scripts with byte parity.
- Simplified configuration, setup, uninstall, doctor, and release contents around the local bridge.
- Added tag-driven GitHub Releases with a verified Windows ZIP, SHA-256 checksum, release evidence, generated notes, and automatic prerelease classification.
- Bounded unauthenticated executor connection cycles to 10 seconds so failed MCP startup cannot reconnect indefinitely.
- Added a copy-ready trusted-agent installation prompt with checksum, MCP registration, restart handoff, and live connection verification requirements.
- Sliced bounded Lua traversals across Roblox scheduler frames, raised the generated request deadline to 30 seconds, and deduplicated re-attached autoexec sessions through executor-global state while retaining Protocol 2.
- Added human-readable tool titles, explicit read-only annotations, object-shaped output schemas, and actionable bounded-retry guidance across the MCP surface.
